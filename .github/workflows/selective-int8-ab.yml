name: Kokoro Vietnamese Selective INT8 APK (manual only)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Model-only experiment notice
        run: |
          echo "Automatic APK builds are intentionally disabled for the current optimization work."
          echo "Use the model-only optimization workflow to produce a drop-in kokoro_vi.onnx artifact."
